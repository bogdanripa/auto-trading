# Journal — August 2026

Append-only. One entry per run. Never rewrite history (ENGINE.md §5).

---

## 2026-08-02 (Sunday) — INCEPTION — bootstrap session

**Run type**: interactive bootstrap (owner present), acting as the inaugural
weekly run. **Market**: closed.

### Account state (live, BT Trade MCP)
- Cash / available for trading: **46,956.13 RON**. Positions: none. Orders: none.
- Baseline fixed: BET 36,154.88 / BET-TR 87,126.43 (2026-07-31 closes) —
  `state/baseline.md`.

### What this session did
1. Studied the deleted spring-2026 engine's git history (facts salvaged,
   design deliberately discarded per owner instruction — it was reset for a
   reason; see LESSONS.md P-1).
2. Ran a 7-agent research sweep: macro regime, BET large caps, mid/small caps
   + defense, market mechanics/tax, H2 2026 catalysts, recent news, old-repo
   distillation.
3. Wrote the engine: ENGINE.md (constitution), STRATEGY.md v1.0, WATCHLIST.md,
   routines/{daily,weekly,monthly}.md, scripts/benchmark.mjs, state files.
4. Scheduled the routines (state/routines.json): daily 06:15 UTC Mon–Fri,
   weekly Sun 14:00 UTC, monthly 1st 07:00 UTC. Owner attached the BT-Trade
   connector + Auto Trader environment via the claude.ai UI.
5. **Fixed the broker gateway**: bt-gateway's permission filter fail-closed on
   unresolved axes *before* instrument resolution, 403-ing every quote/preview/
   order call for the (correctly) restricted API key. Patched assertAllowed
   with per-site axis scoping + market-code canonicalization, pushed to
   bt-gateway main (commit 54829d1), verified end-to-end after deploy:
   TLV quote bid/ask 36.90/36.98; preview 100 TLV = 11.07 RON fee (**0.30%,
   min 2 RON — empirical**).
6. Decided the week-1 plan (STRATEGY.md §5): tranche-1 limit orders Monday in
   SNN (≤64.6, ~3k), TLV (≤36.9, ~3k), IARV (≤38.7, ~2.5k) — ~19% of capital;
   staged entry into a melt-up with a dense autumn risk calendar.

### Deliberation (why this plan and not another)
- Steelman of "do nothing / all cash": BET +47.9% YTD at ATH, recession, no
  government, ratings on the last IG rung — a 25–30% correction scenario is
  openly discussed. Waiting is defensible. Rejected as a *full* posture
  because: the mandate is long-term outperformance, not market timing; the
  May 5 crisis dip was bought within hours (this market's dips are violent
  and short); and the chosen names are the *cheap* residue of the rally
  (8–15x with dividends), not the momentum leaders. ~19% initial exposure +
  staged adds + dislocation playbook is the middle path that keeps both
  errors (chasing and freezing) bounded.
- Steelman of "deploy most of it now": in a continuing melt-up, staged entry
  lags 2x BET badly. Rejected because fresh capital at ATH into names above
  every published analyst target violates the margin-of-safety edge (§3 of
  ENGINE.md); the Aug 10–Oct window offers multiple repricing events to buy
  into with information instead of hope.
- Name selection: SNN/TLV are the two cheapest quality large caps (8.3x/9.9x)
  with catalysts (H1 Aug 14; special-div option autumn). IARV over TTS for
  the third slot: funded structural theme (SAFE €16.68bn) beats a cyclical
  Danube turnaround on thesis strength; TTS stays triggered-watch. Avoided
  all momentum leaders (EL +55% H1, PE +89%, SNG ~37x fwd, SNP 25%+ over
  consensus with zero Buy ratings).

### Assumption ledger (new — grades due per date)
- A1 (Aug 9): staged entry beats full deployment over Aug–Oct — at least one
  ≥3% BET pullback or better prices in target names before Oct 31.
- A2 (Aug 14): SNN H1 confirms Q1 earnings strength.
- A2b (~Aug 31): IARV H1 confirms defense step-up (≥1.5x YoY).
- A3 (Aug 31): TLV H1 keeps FY guidance ≥4.3bn alive.
- A4 (Sep 30): the political/PNRR window produces a tradeable dislocation OR
  resolves benignly — either validates holding reserve.
- A5 (Aug 9): first unattended runs execute cleanly (tools, pushes, Telegram).
- A6 (Aug 9): the 0.30%/min-2-RON fee model holds for real placed orders
  (verify on first fill against actual commission charged).

### Open items handed to the next runs
- Verify 2026 tax rates (CGT 1%/3% bands, dividend 16%) — research agent
  report pending at time of writing; incorporate when available.
- `list_markets` returns empty upstream (BT nomenclature quirk) — harmless
  (symbol resolution works), but note it; do not "fix" what isn't broken.
- First daily run (Mon Aug 3): confirm BT MCP tools present in the Auto
  Trader environment before anything else; confirm Telegram skill exists
  there; place week-1 orders per STRATEGY §5 only if live quotes are within
  bands.
- A paused legacy trigger from the old engine exists (see state/routines.json
  notes) — flagged to owner, not deleted.

### Owner interactions this session
Owner granted: bt-gateway repo access (read→push), gateway MCP, routines
connector/env config via UI. Owner instructed: fresh design (no old-engine
inspiration), routines may commit/push their own changes (self-learning
mechanism), learn from assumptions/mistakes/successes explicitly.
