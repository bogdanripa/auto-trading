# OS
.DS_Store

# Secrets
.env

# Node
node_modules/
package-lock.json

# Claude Code
.claude/settings.local.json

# LocalStore dev fallback (used only when FIRESTORE_PROJECT is unset).
# Authoritative state lives in Firestore; these files are scratch.
.bt_session.json
portfolio/state.json
portfolio/orders.jsonl
portfolio/fills.jsonl
journal/trades.jsonl
considered/
snapshots/
