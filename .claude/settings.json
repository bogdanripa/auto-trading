{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -e .mcp.json ]; then main=\"$(git rev-parse --path-format=absolute --git-common-dir 2>/dev/null)\"; if [ -n \"$main\" ] && [ -f \"$main/../.mcp.json\" ]; then ln -s \"$main/../.mcp.json\" .mcp.json; fi; fi"
          }
        ]
      }
    ]
  }
}
